apiVersion: batch/v1
kind: Job
metadata:
  generateName: jobpresync-
  annotations:
    argocd.argoproj.io/hook: PreSync
spec:
  template:
    metadata:
      name: job
    spec:
      containers:
      - name: presyncjob
        image: busybox                 
        command: ["echo", "job-presync"]   
